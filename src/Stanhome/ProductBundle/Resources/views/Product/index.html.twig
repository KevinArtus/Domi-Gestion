{% extends '::base.html.twig' %}

{% block content %}
    <ol class="breadcrumb">
        <li><a href="{{ path('stanhome_portal_dashboard_index') }}">{{ 'StanhomePortalBundle.global.dashboard'|trans }}</a></li>
        <li class="active"><a href="{{ path('stanhome_product_product_index') }}">{{ 'StanhomeProductBundle.products.page_index.header'|trans }}</a></li>
    </ol>
    <div id="fwp-content" class="col-md-12">
        <div class="row">
            <div class="col-md-2  col-md-offset-1">
                <h2>{{ 'StanhomeProductBundle.products.page_index.header'|trans }}</h2>
            </div>
            {#<div class="col-md-2 right">#}
                {#<a href="{{ path('stanhome_product_product_new') }}"><i class="icon-plus left"></i><i class="icon-icon-project"></i><div>{{ 'StanhomeProductBundle.products.page_index.add'|trans }}</div></a>#}
            {#</div>#}
        </div>
        <div class="container">

            {% if entities is empty %}
    {{ 'StanhomeProductBundle.products.page_index.noProduct'|trans }}
{% else %}
    <div class="panel panel-default table-responsive">
    <table id="tableShipping" class="table table-hover table-responsive table-striped table-bordered">
        <thead>
        <tr>
            <th class="col-md-2">{{ sylius_resource_sort('name', 'StanhomeProductBundle.products.page_index.reference'|trans) }}</th>
            <th class="col-md-2">{{ sylius_resource_sort('name', 'StanhomeProductBundle.products.page_index.libelle'|trans) }}</th>
            <th class="col-md-2">{{ sylius_resource_sort('end_estimated_at', 'StanhomeProductBundle.products.page_index.price'|trans) }}</th>
            <th class="col-md-2">{{ sylius_resource_sort('end_estimated_at', 'StanhomeProductBundle.products.page_index.cat'|trans) }}</th>
            <th>Modifier</th>
            <th>Supprimer</th>
        </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>
                    {{ entity.reference }}
                </td>
                <td><span class="label label-default">{{ entity.libelle }}</span></td>
                <td><span class="label label-default">{{ entity.price }}</span></td>
                <td><span class="label label-default">{{ entity.category.libelle }}</span></td>
                <input type='hidden' name='_method' value='PUT'>
                <td class="center"><a href="{{ path('stanhome_product_product_edit', { 'reference': entity.reference }) }}"><i class="icon-pencil2"></i></a></td>
                <input type='hidden' name='_method' value='DELETE'>
                <td class="center"><a href="{{ path('stanhome_product_product_delete', { 'reference': entity.reference }) }}"><i class="icon-pencil2"></i></a></td>

            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endif %}
            </div>
        </div>


        </div>
    </div>
    </div>
    </div>
{#    <h1>Product list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>Description</th>
                <th>Price</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('stanhome_product_product_index') }}">{{ entity.id }}</a></td>
                <td>{{ entity.libelle }}</td>
                <td>{{ entity.description }}</td>
                <td>{{ entity.price }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('stanhome_product_product_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('stanhome_product_product_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('stanhome_product_product_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>#}
    {% endblock %}
