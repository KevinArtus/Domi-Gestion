{% if customers %}
    <li class="dropdown-header">{% trans %}FimeRHBundle.companies.search_header{% endtrans %}</li>
    {% for customer in customers %}
        <li data-action="{{ path("stanhome_rh_customer_show", {"id": customer.id}) }}"><a href="#"><span class="glyphicon glyphicon glyphicon-briefcase"></span> {{ customer.nom }}</a></li>
    {% endfor %}
{% endif %}